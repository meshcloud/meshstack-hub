version: 1
frontend:
  buildpath: docs
  phases:
    preBuild:
      commands:
       - cd website
       - yarn install --frozen-lockfile
    build:
      commands:
        - yarn build
        - ls -la dist
  artifacts:
    baseDirectory: website/dist
    files:
      - '**/*'
