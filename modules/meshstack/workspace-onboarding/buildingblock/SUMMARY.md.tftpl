# Workspace Onboarding: **${workspace_display_name}**

%{ if expired ~}
⚠️ **Expired and destroyed.** The expiry date (`${workspace_expiry_date}`) for workspace
`${workspace_identifier}` has passed, so this run deleted the workspace, its payment method
(`${payment_method_identifier}`), the project (`${project_identifier}`) and its STACKIT tenant.
Nothing else to do here — order a new instance of this building block if you need a replacement.
%{ else ~}
## Details

| Property | Value |
|----------|-------|
| **Workspace** | `${workspace_identifier}` (expires ${workspace_expiry_date}) |
| **Workspace Owner** | ${workspace_owner} |
| **Payment Method** | `${payment_method_identifier}` |
| **Project** | `${project_identifier}` — ${project_display_name} |
| **Project Admin** | ${project_admin} |
| **STACKIT Project** | ${stackit_project} |

The workspace, payment method, project and STACKIT tenant are ready to use. Manage further access
and resources from inside the new workspace.

Ordering this building block again after `${workspace_expiry_date}` deletes all of the above.
%{ endif ~}
