name: ${workflow_name}

on:
  workflow_dispatch:
    inputs:
      buildingBlockRunUrl:
        description: "URL to fetch the Building Block Run Object from"
        required: true
      MESHSTACK_RUN_TOKEN:
        description: "meshStack run token (ephemeral input)"
        required: true

permissions:
  contents: read

jobs:
  destroy:
    runs-on: ubuntu-latest
    steps:
      - name: Setup meshStack auth from run token
        run: |
          set -o errexit
          set -o nounset
          set -o pipefail

          token_file="$${RUNNER_TEMP:-/tmp}/meshstack_token.json"
          printf '{"token":"%s"}' "$${{ inputs.MESHSTACK_RUN_TOKEN }}" > "$token_file"
          echo "Prepared meshStack token file from MESHSTACK_RUN_TOKEN at $token_file"

      - name: Register source
        id: register-source
        uses: meshcloud/actions-register-source@v2
        with:
          steps: |
            [
              { "id": "simulate-destroy", "displayName": "Simulate Destroy" }
            ]

      - name: Show extracted building block inputs
        run: |
          echo "environment=$${{ steps.register-source.outputs.environment || 'not-provided' }}"

      - name: Update BB Run / simulate-destroy / in progress
        uses: meshcloud/actions-send-status@v2
        with:
          step_id: "simulate-destroy"
          step_status: 'IN_PROGRESS'

      - name: Simulate destroy steps
        id: simulate-destroy
        run: |
          echo "No cloud resources are deleted by this reference workflow."
          echo "Use this job to add your real cleanup logic later."
          echo "## meshStack Workflow Reference - DESTROY" >> "$GITHUB_STEP_SUMMARY"
          echo "- Environment: $${{ steps.register-source.outputs.environment || 'not-provided' }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- Run: $${{ github.server_url }}/$${{ github.repository }}/actions/runs/$${{ github.run_id }}" >> "$GITHUB_STEP_SUMMARY"

      - name: Update BB Run / simulate-destroy / complete
        if: $${{ always() }}
        uses: meshcloud/actions-send-status@v2
        with:
          step_id: "simulate-destroy"
          step_status: $${{ steps['simulate-destroy'].outcome == 'success' && 'SUCCEEDED' || 'FAILED' }}
          user_message: $${{ steps['simulate-destroy'].outcome == 'success' && 'Destroy simulation succeeded.' || 'Destroy simulation failed.' }}
          system_message: $${{ steps['simulate-destroy'].outcome == 'success' && 'Destroy simulation succeeded.' || 'Destroy simulation failed. Check the GitHub Actions log for details.' }}

      - name: Update BB Run / complete
        if: $${{ always() }}
        uses: meshcloud/actions-send-status@v2
        with:
          run_status: $${{ job.status == 'success' && 'SUCCEEDED' || 'FAILED' }}
